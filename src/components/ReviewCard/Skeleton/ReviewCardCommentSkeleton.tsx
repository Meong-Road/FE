import { Skeleton } from "@/components/ui/skeleton";

export default function ReviewCardCommentSkeleton() {
  return (
    <Skeleton
      fontSize="base"
      tabletFontSize="base"
      className="leading-relaxed"
    />
  );
}
