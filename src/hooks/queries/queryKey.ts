import { gatheringsQueryKeys } from "./gatherings/queryKey";
import { reviewsQueryKeys } from "./reviews/queryKey";

export const queryKeys = {
  gatherings: gatheringsQueryKeys,
  reviews: reviewsQueryKeys,
};
